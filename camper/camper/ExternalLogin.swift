import Foundation

class ExternalLoging {
    let name: String
    let state: String
    let url: String
    
    init(name: String, state: String, url: String) {
        self.name = name
        self.state = state
        self.url = url
    }
}